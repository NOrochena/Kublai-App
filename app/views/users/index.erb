<div class="row">
	<div class="eleven wide column">

        <h1 class="ui header">Users</h1>

        <% @users.each do |user| %>
            <h2><%= user.name %> (<%= link_to "View Profile", user_path(user)%>) | Total Posts: <%= user.posts.length %> | Total Comments: <%= user.comments.length %> </h2>
        <% end %>
    </div>

    <div class="five wide column">
    </div>

</div>


