<%= form_for @post do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>

    <%= f.label :content %>
    <%= f.text_area :content %>

    <%= f.label :tag_id %>
    <%= f.collection_select :tag_id, Tag.all, :id, :name %>

    <%= f.submit%>
<% end %>